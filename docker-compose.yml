version: "3.9"

services:
  obiadex-app:
    build:
      context: .
      target: runner
    container_name: obiadex-app
    ports:
      - "80:4321"
    environment:
      HOST: "0.0.0.0"
      PORT: "4321"
    restart: unless-stopped
